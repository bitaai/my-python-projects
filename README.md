 To-Do List Manager

A simple command-line to-do list that stores tasks in a file.

## Features
- Add new task
- View all tasks
- Mark task as done (removes it)
- Delete task
- Persistent storage using `tasks.txt`
  
